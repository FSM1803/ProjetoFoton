<!-- borderradius_edittext.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/cardview_dark_background" /> <!-- Cor de fundo -->
            <corners android:radius="0dp" /> <!-- Raio de borda -->
        </shape>
    </item>
    <item android:top="1dp" android:right="1dp" android:left="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" /> <!-- Cor de fundo da borda (transparente) -->
            <corners android:radius="0dp" /> <!-- Raio de borda -->
            <stroke android:width="2dp" android:color="@color/white" /> <!-- Largura e cor da borda -->
        </shape>
    </item>
</layer-list>
